<UserControl
    x:Class="RecipeDownloader.WinUI.Views.GroceryListView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:vm="using:RecipeDownloader.ViewModels">

    <Grid Padding="12" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Grocery List" Style="{StaticResource SectionHeader}" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="6">
            <TextBlock Text="{Binding Recipe1Title}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock Text="+" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            <TextBlock Text="{Binding Recipe2Title}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
        </StackPanel>

        <ScrollViewer Grid.Row="2">
            <StackPanel Spacing="8">
                <ItemsControl ItemsSource="{Binding Groups}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate x:DataType="vm:GroceryItemGroupViewModel">
                            <StackPanel Margin="0,0,0,12">
                                <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                                        Padding="8,4" CornerRadius="4" Margin="0,0,0,4">
                                    <StackPanel Orientation="Horizontal" Spacing="4">
                                        <TextBlock Text="{Binding CategoryName}" FontWeight="SemiBold" />
                                        <TextBlock Text="(" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        <TextBlock Text="{Binding ItemCount}"
                                                   Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                        <TextBlock Text=")" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                    </StackPanel>
                                </Border>
                                <ItemsControl ItemsSource="{Binding Items}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate x:DataType="vm:GroceryItemViewModel">
                                            <Border Padding="8,4"
                                                    BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                                                    BorderThickness="0,0,0,1">
                                                <Grid ColumnSpacing="4">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="32" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="110" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>

                                                    <CheckBox Grid.Column="0" IsChecked="{Binding IsChecked, Mode=TwoWay}"
                                                              MinWidth="0" VerticalAlignment="Center" />
                                                    <TextBlock Grid.Column="1" Text="{Binding Name}"
                                                               VerticalAlignment="Center" />
                                                    <TextBlock Grid.Column="2" Text="{Binding QuantityDisplay}"
                                                               VerticalAlignment="Center"
                                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                                    <Border Grid.Column="3" Background="#C8E6C9" CornerRadius="3"
                                                            Padding="6,2" VerticalAlignment="Center"
                                                            Visibility="{Binding IsShared, Converter={StaticResource BoolToVis}}">
                                                        <TextBlock Text="shared" FontSize="10" Foreground="#2E7D32" />
                                                    </Border>
                                                </Grid>
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- On-hand section -->
                <StackPanel Visibility="{Binding OnHandItems.Count, Converter={StaticResource BoolToVis}}">
                    <Border Background="#FFF3E0" Padding="8,4" CornerRadius="4" Margin="0,0,0,4">
                        <TextBlock FontWeight="SemiBold" Text="Already On Hand" Foreground="#7A4F01" />
                    </Border>
                    <ItemsControl ItemsSource="{Binding OnHandItems}">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate x:DataType="vm:GroceryItemViewModel">
                                <Border Padding="8,4"
                                        BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                                        BorderThickness="0,0,0,1">
                                    <TextBlock Text="{Binding Name}"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               TextDecorations="Strikethrough" />
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="3" Orientation="Horizontal">
            <Button Content="Copy to Clipboard" Command="{Binding CopyToClipboardCommand}"
                    Style="{StaticResource ActionButton}" />
        </StackPanel>

        <TextBlock Grid.Row="4" Text="{Binding SummaryText}" FontWeight="SemiBold" />
    </Grid>
</UserControl>
